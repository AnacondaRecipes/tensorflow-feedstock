# the conda build parameters to use
build_parameters:
  - "--suppress-variables"
  - "--skip-existing"
  - "--error-overlinking"
  - "--variants \"{skip_cuda_prefect: True}\""
  - "--python=3.12"
  - "--numpy=1.26"

channels:
  - https://staging.continuum.io/prefect/fs/bazel-feedstock/pr19/4562bdf